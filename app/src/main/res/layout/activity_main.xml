<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- EditText para ingresar el valor a convertir -->
    <EditText
        android:id="@+id/editTextValor"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Valor a convertir"
        android:inputType="numberDecimal" />

    <!-- Spinner para seleccionar la moneda de origen -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Seleccione moneda origen:" />
    <Spinner
        android:id="@+id/spinnerOrigen"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <!-- TextView que mostrará el nombre de la moneda origen -->
    <TextView
        android:id="@+id/textViewMonedaOrigen"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Nombre de la moneda origen" />

    <!-- Spinner para seleccionar la moneda de destino -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Seleccione moneda destino:" />
    <Spinner
        android:id="@+id/spinnerDestino"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <!-- TextView que mostrará el nombre de la moneda destino -->
    <TextView
        android:id="@+id/textViewMonedaDestino"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Nombre de la moneda destino" />

    <!-- Botón para convertir -->
    <Button
        android:id="@+id/btnConvertir"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Convertir" />

    <!-- TextView para mostrar el resultado -->
    <TextView
        android:id="@+id/textViewResultado"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Resultado de la conversión"
        android:paddingTop="16dp" />
</LinearLayout>
